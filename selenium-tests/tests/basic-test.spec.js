// problem: the phillipines didn't have anything but priority shipping

var assert = require('assert');

var SELECTORS = {
}

describe('predeployment test', function() {
  it('My first test', function () {
    browser.url(global.testConfig.baseUrl);
    browser.pause(60000);
  });
});

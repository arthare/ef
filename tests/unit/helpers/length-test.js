
import { length } from 'efitness/helpers/length';
import { module, test } from 'qunit';

module('Unit | Helper | length');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = length([42]);
  assert.ok(result);
});

